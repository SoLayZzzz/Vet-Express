import 'dart:ui';

class Translate {
  static final supportedLanguage = [
    const Locale('en', 'US'),
    const Locale('zh', 'ZH'),
    const Locale('km', 'KH'),
  ];

  static const defaultLanguage = Locale('km', 'KH');
  static const englishLanguage = Locale('en', 'US');
  static const chineseLanguage = Locale('zh', 'ZH');
}
